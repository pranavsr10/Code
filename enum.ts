 enum choice {
    A, // i can assign any number other than begining with 0
    B, // i can also assign any strings as value but i have to insert for every choice
    C // i can assign both too
}
const Mychoice = choice.B;

// can put const on the begining of the enum